interface userProfileType {
  edit?: boolean;
  BackWidth: string;
  BackHeight: string;
  IconWidth: string;
  IconHeight: string;
  onClick?: () => void;
  update?: boolean;
  userImg?: string;
  myProfile?: boolean;
}
